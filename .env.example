# =============================================================================
# DeepSeek Harness — Docker 部署环境变量示例
#
# 使用：cp .env.example .env，然后按需填写。
# 注意：.env 已被 .gitignore 排除、不会提交；本文件（.env.example）会随仓库提交，
#       里面只放占位符，绝不填真实 IP / 路径 / 密码。
# =============================================================================

# -----------------------------------------------------------------------------
# 1. GitHub PAT —— dsh-market Gist 备份（可选）
# -----------------------------------------------------------------------------
# 给 dsh-market 的 Settings → Backup → "Sync through private GitHub Gist" 功能用的
# GitHub personal access token。dsh-market 读取顺序：请求 body 临时 token > 本变量 >
# gh auth token CLI（容器内无 gh，留空会触发 "GitHub token is required" 错误）。
# 建议最小 scope：gist（创建/读取私有 gist）。请勿提交到仓库，.env 已在 .gitignore。
DSH_GITHUB_TOKEN=

# -----------------------------------------------------------------------------
# 2. trusted hosts —— settings/credentials 平面放行范围（必需）
# -----------------------------------------------------------------------------
# 上游把 settings/credentials 平面默认锁定为「仅本机回环」；本 fork 通过
# `dsh web --trusted-host <IP>` 将其放行到以下地址。值必须是 IP（可带端口）。
# 内网地址：NAS 的局域网 IP（浏览器访问 http://<内网IP>:3080）
DSH_TRUSTED_HOST_LAN=192.168.1.100
# 公网地址：frp 等转发入口的 IP（浏览器访问 http://<公网IP>:3080）
DSH_TRUSTED_HOST_PUBLIC=1.2.3.4

# -----------------------------------------------------------------------------
# 3. 宿主外挂路径（compose volumes 用）
# -----------------------------------------------------------------------------
# NAS 侧数据根目录：所有 bind-mount 源路径的公共前缀。docker daemon 跑在 NAS 上，
# 路径必须是 NAS 视角（/vol1/...），不能用 agent 容器内的 /home/agent/... 视角。
HOST_APP_DATA=/vol1/1000/appData
